# ⚙️ Где указывать данные для подключения к БД

## 📍 Ответ: config/env.local.php

Все настройки хранятся в файле:
```
config/env.local.php
```

---

## 🚀 Быстрая настройка

### 1. Файл уже создан ✅

Я уже создал файл `config/env.local.php` с вашими данными:

```php
<?php
return [
    // Database ← ВОТ ЗДЕСЬ УКАЗАНЫ ДАННЫЕ БД!
    'DB_HOST'    => 'localhost',
    'DB_NAME'    => 'cv82602_slimdev',
    'DB_USER'    => 'cv82602_slimdev',
    'DB_PASS'    => '4YxsN8Pp',
    
    // JWT и другие настройки
    'JWT_SECRET' => 'your-jwt-secret-change-me',
    'HASH_KEY'   => 'your-hash-key-change-me',
];
```

### 2. Обновлен index.php ✅

Теперь `public_html/index.php` использует конфигурацию вместо захардкоженных данных:

```php
// Было (ПЛОХО):
$pdo = new PDO('mysql:host=localhost;dbname=cv82602_slimdev','cv82602_slimdev','4YxsN8Pp');

// Стало (ХОРОШО):
$config = new \Config();
$dsn = "mysql:host={$config->host};dbname={$config->db_name}";
$pdo = new PDO($dsn, $config->username, $config->password);
```

---

## 🔧 Как изменить данные БД

### Локально (на вашем компьютере)

Отредактируйте `config/env.local.php`:

```php
return [
    'DB_HOST' => 'localhost',
    'DB_NAME' => 'my_local_db',      // ← измените здесь
    'DB_USER' => 'root',              // ← и здесь
    'DB_PASS' => '',                  // ← и здесь
];
```

### На сервере

Загрузите обновленный `config/env.local.php` через FTP/SFTP в:
```
/home/c/cv82602/tradeApp/config/env.local.php
```

Или создайте на сервере через SSH:
```bash
nano /home/c/cv82602/tradeApp/config/env.local.php
```

---

## ✅ Проверка конфигурации

Запустите тестовый скрипт:

```bash
php test-config.php
```

Вывод:
```
✅ Конфигурация загружена
✅ Подключение к БД успешно!
✅ identity_users (записей: 0)
✅ identity_session (записей: 0)
✅ ref_country (записей: 8)
✅ storage_files (записей: 0)
```

---

## 🔐 Безопасность

### ✅ Что уже настроено:

1. **Файл защищен в .gitignore**
   - `config/env.local.php` не попадет в Git

2. **Есть файл-пример**
   - `config/env.local.example.php` - для других разработчиков

3. **Отдельные конфиги для разных окружений**
   - На сервере - одни данные
   - Локально - другие данные

### ⚠️ Что нужно сделать:

**Смените секретные ключи на случайные!**

```php
// В config/env.local.php
'JWT_SECRET' => 'your-jwt-secret-change-me',  // ← СМЕНИТЕ!
'HASH_KEY'   => 'your-hash-key-change-me',    // ← СМЕНИТЕ!
```

**Генерация случайного ключа:**

```bash
# В терминале
php -r "echo bin2hex(random_bytes(32)) . PHP_EOL;"
```

Результат:
```
5f4dcc3b5aa765d61d8327deb882cf99a1b5f5e4e4e4e4e4e4e4e4e4e4e4e4e4
```

Вставьте в конфиг:
```php
'JWT_SECRET' => '5f4dcc3b5aa765d61d8327deb882cf99a1b5f5e4e4e4e4e4e4e4e4e4e4e4e4e4',
```

---

## 📋 Все доступные параметры

```php
<?php
return [
    // Приложение
    'APP_NAME'   => 'Backend REST API',
    'APP_VERSION'=> '1.0.0',

    // Identity / JWT
    'HASH_KEY'   => 'random-hash-key-here',
    'CLIENT_IDS' => 'web_app,mobile_app',      // через запятую
    'JWT_SECRET' => 'random-jwt-secret-here',

    // База данных
    'DB_HOST'    => 'localhost',
    'DB_NAME'    => 'cv82602_slimdev',
    'DB_USER'    => 'cv82602_slimdev',
    'DB_PASS'    => '4YxsN8Pp',
];
```

---

## 🆘 Проблемы и решения

### Ошибка: "Access denied"

**Решение:** Проверьте данные в `config/env.local.php`

```bash
# Проверьте подключение вручную
mysql -u cv82602_slimdev -p cv82602_slimdev
```

### Ошибка: "Unknown database"

**Решение:** Создайте БД или выполните миграции

```bash
mysql -u cv82602_slimdev -p cv82602_slimdev < sql/install_all.sql
```

### Конфигурация не работает

**Решение:** Убедитесь что файл существует

```bash
ls -la config/env.local.php
```

Если нет - скопируйте:
```bash
cp config/env.local.example.php config/env.local.php
```

---

## 📚 Подробная документация

Детальная инструкция в файле:
```
docs/CONFIGURATION.md
```

---

## ✨ Итого

**Теперь все данные БД в одном месте:**

```
config/env.local.php  ← ВОТ ЗДЕСЬ!
```

**Преимущества:**
- ✅ Безопасно (не в Git)
- ✅ Удобно (один файл)
- ✅ Гибко (разные конфиги для dev/prod)
- ✅ Правильно (best practice)

**Больше никаких захардкоженных паролей в коде!** 🎉
